# Core
pandas>=2.2
numpy>=1.26
scipy>=1.13
pyarrow>=16.0        # to read the .parquet files written by the R extraction scripts

# ML / stats (used from Phase 3 onward, listed here for completeness)
scikit-learn>=1.5
xgboost>=2.0
shap>=0.45
statsmodels>=0.14

# Dimensionality reduction / QC visualization (Phase 1, item 4)
umap-learn>=0.5
matplotlib>=3.9
seaborn>=0.13
plotly>=5.22          # optional, for an interactive PCA/UMAP audit plot

# Gene ID handling (Phase 1, item 3)
pybiomart>=0.2        # optional cross-check against Ensembl; not required if biomaRt (R) output is reused
xgboost>=2.0
